export const defaultHeaders = () => {
    const defaultHeaders: { [key: string]: string | undefined } = {
        accept: 'application/json',
        // Any other default headers can be added here
    };

    return defaultHeaders;
};
